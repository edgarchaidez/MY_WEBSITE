import React from 'react';
import "./Header.css";
import Nav from "./Nav";

const header = (props) => {
    return (
        <header className = "header-style">
            <div></div>
        </header>
    );
}

export default header;
